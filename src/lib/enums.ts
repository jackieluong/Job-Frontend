export enum JobStatus {
    PENDING = 'PENDING',
    ACTIVE = 'ACTIVE',
    CLOSED = 'CLOSED',
    REJECTED = 'REJECTED',
}

export enum CVStatus {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
    SEEN = 'SEEN',
}