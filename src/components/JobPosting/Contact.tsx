import React from 'react';

type ContactProps = {
    // Define your props here
};

export default function Contact(props: ContactProps) {
    return (
        <div>
            Contact
        </div>
    );
}